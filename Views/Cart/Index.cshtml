@{
    ViewData["Title"] = "Cart Page";
}
@model List<CartViewModel>



<div class="product-container">
    @if (Model.Count > 0)
    {
        @foreach (var product in Model)
        {           
                <div class="product-card">

                    <h2 class="text-primary">@product.Product.Name</h2><br /><br />

                    <label class="text-danger">Count: </label>
                    <label class="text-black-50">@product.Count</label><br />

                    <label class="text-danger">Price:</label>
                    <label class="text-black-50">@product.Price</label><br />
                   
                    @*<img src="@Url.Content($"data:image/png;base64, {Convert.ToBase64String(product.Product.Pic)}")" /><br />*@
                    
                    
                </div>


                    @*<form asp-controller="Account" asp-action="Buy" method="post">
                        <input type="hidden" name="productId" value="@product.Id" />
                        <div class="form-group d-inline-flex marginLeft-20">
                            <label for="count">Count:</label>
                            <button type="submit" class="btn btn-danger">Buy</button>
                            <input type="number" class="form-control count-width" name="count" placeholder="Count" required />
                        </div>

                    </form>*@
        }
    }
    else
    {
        <h1 class="align-self-center">Cart is empty.</h1>
    }
</div>
